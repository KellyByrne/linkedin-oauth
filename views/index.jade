extends layout

block content
  h1= title
  p Welcome to #{title}
   if user
      p
        |Hello #{user.displayName} You're logged in
        ||
        a(href="/logout") Logout
    else
      p
        a(href="/auth/linkedin") Login